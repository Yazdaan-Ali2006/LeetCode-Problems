class Solution {
public:
    bool uniformArray(vector<int>& nums1) {
      //if all of them are even or odd=true
      //if it is the case of even +odd then by subtraction i9 can make the whole parity same so true here aswell
        
        return true;
        
    }
};